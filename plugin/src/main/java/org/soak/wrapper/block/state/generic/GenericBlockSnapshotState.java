package org.soak.wrapper.block.state.generic;

import org.soak.wrapper.block.state.AbstractBlockSnapshotState;
import org.spongepowered.api.block.BlockSnapshot;

public class GenericBlockSnapshotState extends AbstractBlockSnapshotState {
    public GenericBlockSnapshotState(BlockSnapshot snapshot) {
        super(snapshot);
    }
}
