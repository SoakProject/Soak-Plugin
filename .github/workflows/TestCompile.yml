name: Test Compile
on: [ push, workflow_dispatch ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '21' ]
    permissions:
      actions: write
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: ${{matrix.java}}
          cache: 'gradle'
      - name: Make gradle executable
        run: chmod +x ./gradlew
      - name: Build Soak Libraries
        run: ./gradlew build
      - name: Build Soak plugin
        run: ./gradlew build
      - name: Upload plugin
        uses: actions/upload-artifact@v4
        with:
          name: Soak.zip
          path: Launch-External/build/libs/Soak.jar
          if-no-files-found: error

